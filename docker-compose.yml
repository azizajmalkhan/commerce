# version nothing but .yml version
version :  '3.8' 

#build: ./orders-service tells buid the docker image from ordes-service directory 
services:
 orders-service:
 build: ./orders-service
 ports:
 - "3001:3001"
 env_file: ./orders-service/.env
